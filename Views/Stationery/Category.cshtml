@model IEnumerable<Group5.Models.Category>

@{
    ViewData["Title"] = "Category";
}
@Html.ValidationSummary(true, "You cannot do this", new { @class = "text-danger" })
@if (TempData.ContainsKey("success"))
{
    <div class="alert alert-success" style="font-size:25px">
        @TempData["success"]
    </div>
}

@if (TempData.ContainsKey("error"))
{
    <div class="alert alert-danger">
        @TempData["error"]
    </div>
}
@if (TempData.ContainsKey("message"))
{
    <div class="alert alert-danger">
        @TempData["message"]
    </div>
}

<p>
    <a asp-action="CreateCate" class="btn btn-danger confirm" style="width:220px;height:45px;font-size:20px" >Create New Category</a>
</p>
<h1 style="text-align:center">Category List</h1>
<table class="table table-cl" id="Category">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
           @*  <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TypeOfQuantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categories)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand)
            </th> *@
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
             <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ImageUrl)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TypeOfQuantity)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.Brand.Id)
            </td> *@
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
            <td>
                    <a asp-action="EditCate" asp-route-id="@item.Id" class="btn btn-success">Edit</a> |
                    <a asp-action="DetailsCate" asp-route-id="@item.Id" class="btn btn-info">Stationery</a> |
                    <a asp-action="DeleteCate" asp-route-id="@item.Id" class="btn btn-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
